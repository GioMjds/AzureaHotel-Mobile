import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function ConfirmAreaBookingScreen() {
    return (
        <SafeAreaView>
            <Text>Confirm Area Booking</Text>
        </SafeAreaView>
    )
}