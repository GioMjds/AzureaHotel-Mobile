import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function ConfirmRoomBookingScreen() {
    return (
        <SafeAreaView>
            <Text>Confirm Room Booking</Text>
        </SafeAreaView>
    )
}