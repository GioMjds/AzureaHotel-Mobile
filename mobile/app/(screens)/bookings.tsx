import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function BookingsScreen() {
    return (
        <SafeAreaView>
            <Text className="text-xl font-bold p-4">My Bookings</Text>
        </SafeAreaView>
    )
}