import StyledText from "@/components/ui/StyledText";
import { SafeAreaView } from "react-native-safe-area-context";

export default function NotificationScreen() {
    return (
        <SafeAreaView>
            <StyledText variant="playfair-medium">
                Notifications Screen
            </StyledText>
        </SafeAreaView>
    )
}