import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function AreasScreen() {
    return (
        <SafeAreaView>
            <Text>Areas Screen</Text>
        </SafeAreaView>
    )
}